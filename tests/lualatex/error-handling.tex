\documentclass{article}
\usepackage{luakeys}
\begin{document}

\directlua{
  luakeys = require('luakeys')
  luakeys_parser = luakeys.build_parser({
    key = { choices = 'one',}
  })
  luakeys_parser:match('key=one')
}

Error
\end{document}
