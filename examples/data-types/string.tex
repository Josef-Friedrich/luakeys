\documentclass{article}
\usepackage{luakeys-debug}

\begin{document}
\luakeysdebug{
  without quotes = no commas and equal signs are allowed,
  with double quotes = ", and = are allowed",
  escape quotes = "a quote \" sign",
  curly braces = "balanced {} curly braces",
}

\end{document}
